/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.pixabay.com','encrypted-tbn1.gstatic.com','encrypted-tbn2.gstatic.com'],
    },
  };
  
  export default nextConfig;
  